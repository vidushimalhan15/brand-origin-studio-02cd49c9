// ─────────────────────────────────────────────────────────────────────────────
//  EDIT ME  —  everything personal lives here.
// ─────────────────────────────────────────────────────────────────────────────

/** Shown on the landing screen: "Happy Birthday <NAME>." */
export const RECIPIENT_NAME = "My Love";

/** Landing screen tagline under the birthday line. */
export const TAGLINE = "The world, according to us.";

/** Button that launches the globe. */
export const START_LABEL = "Spin the globe";

/**
 * The final overlay shown once every memory pin has been opened.
 * Replace this with your closing message — line breaks are respected.
 */
export const CLOSING_MESSAGE = `You've seen every place we've been.
But this map isn't finished — it's just the pins we've dropped so far.

Here's to every coordinate still ahead of us.

Happy birthday. I love you.`;

/** Signature line under the closing message (set to "" to hide). */
export const CLOSING_SIGNATURE = "— always, me";
